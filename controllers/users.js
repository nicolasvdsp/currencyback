const getAll = (req, res, next) => {
  res.send("respond with a resource");
};

const create = (req, res, next) => {
  res.send("respond with a resource");
};

module.exports = {
  getAll,
  create,
};
